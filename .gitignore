__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.venv/
build/
dist/
.coverage
*.log
~/.codelet/sessions/

# Per-project session registry (machine-local; references global snapshots)
.codelet/sessions.json

# Eval run reports
evals/reports/
evals/swebench/out/
sb-cli-reports/

# Secrets -- the file may contain real API keys. The committed `.env.example`
# is the template.
.env
.env.local
